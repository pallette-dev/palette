/**
 * Types for `@pallette/catalog/ce` (runtime is `dist/ce.js`, built from `ce-bundle-entry.ts`).
 * This file must not import `./customElement` or other `.tsx`, or TypeScript / bundlers can
 * follow types into React sources in Svelte-only apps.
 */
export declare function registerPalletteCatalog(tagName?: string): void;
